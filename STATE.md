# OMEGA Project State

## Continuity anchor

- Project path: `C:\Users\Eng-Mohamed Hasan\Documents\Codex\Impossible-Machine-OMEGA`
- Current verified version: `0.21.0` (Usability Hardening)
- Starting baseline for this increment: `0.11.0`
- Storage: local SQLite, schema version 7, migrated in place
- Runtime: Python 3.11+ standard library; no third-party runtime dependency

## Architecture

`omega.store` owns transactional SQLite persistence, migrations, canonical portability, and append-only audit events. `omega.engine` owns deterministic graph operations. `omega.api` serves both the JSON API and the packaged `omega/web` workspace. The browser workspace uses only the API and therefore exercises the same Core contracts as CLI and integration tests.

## Real-World Value Engine

### ZRWVE V1.2J public reality watch

The existing Wake Plane now contains a bounded `ACTIVE_READ_ONLY` Reality Watch for T2 dataflow partial-resume/state-reconstruction incidents. Its registry is limited to `PrefectHQ/prefect` and `apache/airflow` public issue APIs; conditional ETag requests, updated-at cursors, source backoff, append-only incident versions, provenance, dedupe, and resource budgets are enforced. Historical replay passed on four structural cases and rejected two simple/generic cases. Live canary evidence currently contains no qualified incident; no Wake request was emitted. B3 and Minimal ZERO comparisons use identical decision-time hashes and never consume outcome truth; missing facts remain narrow `HUMAN_EVIDENCE_REQUIRED` contracts only when a candidate is otherwise comparison-ready. This technical watch does not change V0.30, market evidence, demand, or economic value: current evidence is L0 and value is 0 KWD.

ZRWVE V1 is a bounded deterministic discovery and falsification layer, not a second Supervisor or external-action system. It keeps L0-L7 evidence distinct, requires provenance before value promotion, freezes experiment thresholds and hashes, compares every proposed primitive against the strongest ordinary baseline, and parks hypotheses that fail to change a decision. `value-cycle` may persist one bounded internal cycle; `value-status`, `value-opportunities`, `value-experiments`, and `value-evidence` are read-only. The first cycle killed the cross-host recovery-conformance hypothesis for baseline parity. No primary value opportunity is active; verified evidence remains L0 and verified economic value remains 0 KWD.

ZRWVE V1.1 expanded the search once into adjacent operational domains while preserving prior semantic exclusions. `value-frontier-cycle` performs the bounded, local multi-baseline falsification and `value-frontier-status` reads its result. Eight candidates were reduced to one GitOps continuation experiment; deterministic controls plus human review matched ZERO on all four historical decisions with equal attention/reliability and lower complexity. The result is `NO_UNDEFEATED_OPPORTUNITY_FOUND`; the current frontier is saturated for the compiled evidence and the engine remains parked until a material evidence, capability, baseline, actor, or workflow change.

## ZRWVE V1.2G qualified participant discovery

`python -m omega.cli value-deep-participant-discovery` performs a bounded read-only search for attributable, independent T2 incident participants without enriching private contact data or opening external authority. Six serious public incident authors were resolved and ranked; all six are `QUALIFIED_BUT_NOT_CONTACTABLE` because only public GitHub profile routes were verified. No participant was selected or bound, no address was inferred, and no external write or message occurred. The cycle is saturated for the current T2 corpus and parks at `PARK_UNTIL_NEW_PUBLIC_EVIDENCE`. It preserves packet hash `80b2e727d47a0ef01bd6c849ecb952a0334ab2789f2cba161fdb5ab1ef75ecae`, initial-message hash `94ea415e20ee755cbf2cdd3ceb751ae674d0dfac61694ba2d2a1da7dafdd966c`, and participant-set hash `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`. Targeted/CLI verification is 15/15 and the full suite is 425/425 with ResourceWarning-as-error; evidence remains L0, V0.30 remains `WAITING_EXTERNAL_EVIDENCE`, and economic value remains 0 KWD.

## ZRWVE V1.2H passive intake design

The V1.2H design cycle compared six passive surfaces and selected one minimal GitHub Issue Form with Stage 1 voluntary qualification and optional Stage 2 E1/E2/E3/E4 packet submission. Provenance must come from a trusted source observation; participant text cannot assert independence. Public attribution and internal hashed dedupe identity remain separate. Opinion, spam, owner/bot/OMEGA/anonymous origins, duplicates, secrets/private data, and prompt injection fail closed. Complete packets reuse the unchanged V1.2E validators and blind B3-versus-minimal-ZERO transform; no evidence requirement is lowered and no submission becomes demand, customer, usage, WTP, value, or revenue. The original packet remains immutable as design evidence.

## ZRWVE V1.2H publication state

The exact bounded publication authorization is consumed and closed. The separate public repository contains only the authorized new Issue Form at commit `bac95d4eafe3180638d90694539e902aa375b723`; the public content and frozen packet hashes were independently verified. The existing Wake Plane now has a distinct `ZRWVE_V1_2H_FAIL_CLOSED` read-only route and is running in `PASSIVE_PRODUCTION`. No submission currently exists. Publication remains an external action only—not discovery, T2 evidence, demand, or value—and the absence of submissions cannot be used as negative T2 evidence until independent exposure is established.

## ZRWVE V1.2D deep reality acquisition

`value-deep-cycle` extends the existing engine with a deterministic twelve-pass analysis of exactly T1 GitOps partial deployment/revision continuity, T2 dataflow partial execution/state resume, and T3 backup/restore truth. The frozen corpus has 23 primary public records (15 incidents, 8 baseline documents); all source facts are DATA_ONLY, deduplicated, and unknown timing/configuration is preserved. T1 and T3 are `BASELINE_WINS`; T2 is `EXTERNAL_VALIDATION_READY` but its B3 configuration, operator burden, and decision delta are unobserved. The final causal decision is `EXTERNAL_INCIDENT_VALIDATION_REQUIRED`, not a product or market claim. A max-three-participant sanitized T2 incident packet and blind B3-versus-minimal-ZERO spec are prepared but not authorized or executed. Evidence remains L0, economic value 0 KWD, Wake Plane PASSIVE_PRODUCTION, Capability Fabric SHADOW, and production default LEGACY. Deep 20/20, related 125/125, and full 391/391 verification passed with ResourceWarning-as-error; compile, benchmark, release, stability, secret, and diff gates passed. Host verification confirms cycle/memory/host hashes agree.

## ZRWVE V1.2E packet hardening

The V1.2E packet-hardening cycle is complete and local-only. `python -m omega.cli value-deep-packet-audit` validates a frozen E1/E2/E3/E4 incident contract (real sanitized incident, actual B3 configuration, ordered operator judgment trace, and externally sourced verification criterion), causal linkage, provenance/privacy, decision/outcome separation, blind outcome mapping, participant limits, and fail-closed stop rules. Packet cycle `zrwve-packet-hardening-0001` is `READY_FOR_OWNER_AUTHORIZATION`; the authority envelope is closed and no external action occurred. The cycle, memory, and host-verification records agree on hash `80b2e727d47a0ef01bd6c849ecb952a0334ab2789f2cba161fdb5ab1ef75ecae`. Packet 16/16, CLI 7/7, deep 20/20, related ZRWVE 51/51, and full 408/408 ResourceWarning-as-error verification passed. Evidence remains L0, economic value 0 KWD, V0.30 `WAITING_EXTERNAL_EVIDENCE`, and no version or production-default change is justified.

## ZRWVE V1.2F channel and participant binding

`python -m omega.cli value-deep-binding-audit` performs a local, non-sending binding audit. It separates configured channel capability from experiment authority, preserves E2-01 as a distinct binding, ranks only public T2 incident records, and fails closed when an attributable independent participant and legitimate one-to-one route are absent. The current frozen result is `READY_TO_BIND_BUT_NO_QUALIFIED_PARTICIPANT`: Gmail is suitable as a transport capability, six candidates were rejected for missing attributable contact routes, zero participants were bound, and `EXTERNAL_ACTION_AUTHORIZED`/external writes remain false. The ZRWVE-only namespace, no-follow-up/no-secrets/no-financial-authority policy, dedupe, expiry, and participant-set/message/packet hashes are persisted under `.omega/zero/`. Host verification is recorded in `zrwve_channel_binding_host_verification_0001.json`; targeted binding/CLI tests pass 17/17 and the full suite passes 418/418 with ResourceWarning-as-error. Evidence remains L0, V0.30 remains `WAITING_EXTERNAL_EVIDENCE`, and economic value remains 0 KWD.

## V0.21 completed

- Facts / Assumptions / Constraints / Unknowns persist end-to-end.
- Directed `depends_on`, `supports`, `contradicts`, and `relates_to` relationships form the Problem/Assumption Graph.
- WHY, BREAK IT, PROVE IT, and WHAT IF run from UI to API to Core.
- Claims store structured evidence, confidence, assumptions, uncertainty, and an explicit falsifier.
- Projects survive process shutdown and reopen from the same database.
- The local workspace adds, edits, and safely deletes nodes and relationships; destructive operations require confirmation.
- The selectable graph has a claim inspector, typed visual language, directed relationship lines, zoom, pan, and fit-to-view.
- WHY, BREAK IT, PROVE IT, and WHAT IF render as human-readable cards and sections; raw JSON is developer-only.
- 60 unit/integration/smoke tests, all benchmarks, 5/5 release gates, and 11/11 stability gates pass.

## Intentionally incomplete

- V1 usefulness remains unproven until independently collected user-outcome evidence and external blind-evaluation records exist.
- Authentication, multi-user sync, hosted deployment, WOS, Reality Compiler, GOD MODE, GENESIS, PROMETHEUS, and HORIZON remain outside Core Alpha.

## Run

```powershell
cd "C:\Users\Eng-Mohamed Hasan\Documents\Codex\Impossible-Machine-OMEGA"
python -m omega.cli --db data/omega.db serve
```

Open `http://127.0.0.1:8787/`.

## Verify

```powershell
python -m unittest discover -s tests -v
python -m omega.cli benchmark
python -m omega.cli release-check
python -m omega.cli --db data/omega-self.db stability-audit
```

## Next milestone

V0.30 External Evaluator Evidence Collection remains the active milestone. LZC V1.15 trusted provenance and
read-only GitHub inbound detection are installed and operating in passive mode; no external evaluator event
is present. The next executable action is to observe a real, independently attributable inbound event or
evaluator submission without changing existing experiment criteria.

## Autonomous development governor

The deterministic Development Governor is an internal, read-only decision aid; it is not a second
Supervisor. `python -m omega.cli development-governor` reads repository state and prior evidence, ranks
bottlenecks, and writes a compact source-hashed evolution checkpoint. It performs no execution, authority
grant, model call, network access, or external action. The latest cycle selected `EVOLUTION_MEMORY_COMPRESSION`
while preserving the primary external-evidence bottleneck, V0.30 waiting state, L0, and 0 KWD.

## Capability Fabric

Capability Fabric V1 is an isolated, deterministic SHADOW layer. It discovers
actual repository/host capabilities, profiles task requirements, scores routes
with transparent factors, and emits explicit fallback and verification plans.
It does not execute providers, call models, access the network, grant authority,
or replace the Supervisor, Wake Plane, AgentBackend, or Host Verification.

The current cycles replayed nine historical work classes and three controlled
low-risk internal routes with safe classification and shadow decision parity.
No measurable decision delta has been established; production remains Legacy.
The Codex CLI executable is visible but its existing provider checkpoint is
`WAITING_RESOURCE`; research, multimodal, and long-horizon adapters remain
`UNKNOWN`. Capability Fabric/CLI tests pass 18/18 and the full suite passes
337/337 with ResourceWarning-as-error; benchmark, release 5/5, stability 11/11,
and the concrete-secret boundary pass. Artifacts are under
`.omega/zero/capability_fabric_*.json`.
